spring:
  config:
    import: optional:classpath:quizup-defaults.yml
  profiles:
    default: local

  application:
    name: quizup-game

  flyway:
    locations: classpath:db/migration/game

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

microservice:
  swagger:
    description: QuizUp Game Module - Game sessions and gameplay management
  websocket:
    # Surface REST/WS portée par le BFF ; les services sont headless.
    enabled: false

