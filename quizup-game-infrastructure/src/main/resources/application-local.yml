server:
  port: 8082

spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:postgresql://localhost:5432/quizup_game
    username: postgres
    password: postgres

logging:
  level:
    io.github.quizup: DEBUG

