# quizup-game (staging local)

Structure cible en 2 sous-modules Maven :

- `quizup-game-domain`
- `quizup-game-infrastructure`

Seul `quizup-game-domain` est importable inter-services.

Le code applicatif reste organise sous `io.github.quizup.game.application.*` dans le module infrastructure.

